name: consulta_saldo_transcaribe
description: Aplicación Flutter para consultar saldo de tarjeta Transcaribe
publish_to: 'none' # No se publicará en pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0
  webview_flutter: ^4.0.7
  cupertino_icons: ^1.0.8
  google_fonts: ^4.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
